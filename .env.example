# Yoto API OAuth2 client ID (required)
YOTO_CLIENT_ID=

# Server port (default: 3000)
PORT=3000

# Directory for state store and token persistence (default: ./data)
DATA_DIR=./data

# Log level: fatal, error, warn, info, debug, trace (default: info)
LOG_LEVEL=info
